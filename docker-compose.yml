version: '3.8'

services:
  mongodb:
    image: mongo:4.4
    container_name: mini-wallet-mongo
    ports:
      - "27017:27017"
    command: [ "--replSet", "rs0", "--bind_ip_all" ]
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
